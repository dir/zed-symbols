{
  "name": "zed-symbols",
  "version": "0.0.25",
  "description": "A simple file icon theme for Zed.",
  "type": "module",
  "scripts": {
    "build": "npx tsx src/build.ts",
    "dev": "npx tsx src/build.ts --watch",
    "sync:from-vscode": "npm --prefix ../vscode-symbols run sync:upstream && npm run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "@types/node": "^22.14.1"
  },
  "peerDependencies": {
    "typescript": "5"
  }
}
